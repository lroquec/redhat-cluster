[Unit]
Description=Apache Tomcat Service
After=network.target

[Service]
Type=forking
User={{ tomcat_user }}
ExecStart={{ tomcat_start_script }}
ExecStop={{ tomcat_stop_script }}
Restart=on-failure

[Install]
WantedBy=multi-user.target